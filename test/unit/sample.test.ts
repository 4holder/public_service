import { expect } from "chai";
import { resolvers} from "../../src/resolvers";

describe("Remove me", () => {
	it("should pass", () => {
		expect(resolvers.Query.itWorks(
			null,
			null,
			null
		)).equal("Yes, it works!");
	})
});